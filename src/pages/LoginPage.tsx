import { Box } from "@mui/material";
import LoginForm from "../components/auth/LoginForm";

function LoginPage() {
  return (
    <Box py={5}>
      <LoginForm />
    </Box>
  );
}
export default LoginPage;
